<Window x:Class="PDF_Editor_Concept.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:pe="clr-namespace:PDFEditorNS;assembly=PDFEditor"
        mc:Ignorable="d"
        Title="MainWindow" Height="840" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Command="{Binding OpenCommand}">Open</Button>
        </StackPanel>
        <pe:PDFEditor x:Name="myEditor" Grid.Row="1" CurrentDoc="{Binding Path=currentPDF, Mode=TwoWay}" />
    </Grid>
</Window>
